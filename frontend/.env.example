# Example environment variables for local development
# Client-visible variables must be prefixed with VITE_

# Base URL for the backend API (frontend will call this from the browser)
VITE_API_BASE_URL=http://localhost:8080/api

# Optional: dev server port for frontend (used by some workflows)
VITE_DEV_SERVER_PORT=5173

# App title / display name
VITE_APP_TITLE="Task Tracker"

# NOTE: Never put secrets (JWT secret, DB password etc.) in variables prefixed with VITE_ if you don't want them exposed to the browser.
